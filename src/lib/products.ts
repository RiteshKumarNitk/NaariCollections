import type { Product } from './types';

// This file is now deprecated since products are managed in Firestore.
// It's kept for historical reference but is no longer used by the application.
export const products: Product[] = [];
